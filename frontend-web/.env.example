# ── Firebase Web SDK ──────────────────────────────────────────────────────────
# 從 Firebase Console → 專案設定 → 您的應用程式（Web）取得
VITE_FIREBASE_API_KEY=AIzaSy...
VITE_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your-project-id
VITE_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=123456789
VITE_FIREBASE_APP_ID=1:123456789:web:abcdef

# ── Vite Dev Proxy（不加 VITE_ 前綴，不會暴露給瀏覽器）────────────────────────
# 預設值：localhost:8118（backend）、localhost:7880（LiveKit）
# 若 backend 或 LiveKit 跑在非預設 port 才需設定
# BACKEND_PROXY_URL=http://localhost:8118
# LIVEKIT_PROXY_URL=http://localhost:7880

# ── Backend API ────────────────────────────────────────────────────────────────
# 本地開發：不需要設定（vite.config.ts 的 proxy 自動轉發到 BACKEND_PROXY_URL）
# 生產環境（Cloudflare Pages）：填入後端的公開網域
# VITE_API_BASE_URL=https://your-backend-domain.com

# ── LiveKit ────────────────────────────────────────────────────────────────────
# 本地開發：不需要設定（由後端 /sessions/:id/token 回傳 URL）
# 生產環境（Cloudflare Pages）：填入 LiveKit Cloud URL
# VITE_LIVEKIT_URL=wss://your-project.livekit.cloud
