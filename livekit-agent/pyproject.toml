[project]
name = "live-english-tutor-agent"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    # STT/TTS disabled — only openai (Ollama LLM) extra is active
    # To re-enable: "livekit-agents[deepgram,silero,openai,cartesia]~=1.0"
    "livekit-agents[openai]~=1.0",
    "httpx==0.27.2",
    "python-dotenv==1.0.1",
]

[tool.uv]
package = false
